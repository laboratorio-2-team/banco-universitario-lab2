import { createSlice } from "@reduxjs/toolkit";

export const movementsSlice = createSlice({
  name: "user",
  initialState: {
    user: null,
  },
  reducers: {},

  extraReducers: (builder) => {},
});
