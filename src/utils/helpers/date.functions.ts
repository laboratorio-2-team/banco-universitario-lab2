import { FormatDate } from "../../interfaces/date.interface";

export const formatDate = (
  date: string,
  actualFormat: FormatDate,
  newFormate: FormatDate
) => {
  console.log({ date, actualFormat, newFormate });

  return "";
};
